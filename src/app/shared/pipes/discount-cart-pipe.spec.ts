import { DiscountCartPipe } from './discount-cart-pipe';

describe('DiscountCartPipe', () => {
  it('create an instance', () => {
    const pipe = new DiscountCartPipe();
    expect(pipe).toBeTruthy();
  });
});
