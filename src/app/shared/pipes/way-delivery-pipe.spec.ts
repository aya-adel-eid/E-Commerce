import { WayDeliveryPipe } from './way-delivery-pipe';

describe('WayDeliveryPipe', () => {
  it('create an instance', () => {
    const pipe = new WayDeliveryPipe();
    expect(pipe).toBeTruthy();
  });
});
